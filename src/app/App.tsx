import { BrowserRouter } from "react-router-dom";
import { Layout } from "./Layout";

const App = () => {
  return (
    <BrowserRouter>
      <Layout>123</Layout>
    </BrowserRouter>
  );
};

export default App;
